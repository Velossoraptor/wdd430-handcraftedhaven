// src/app/dashboard/settings/page.tsx

// Import the component that contains both Profile and Settings logic
import SellerProfilePage from '@/app/dashboard/profile/page';

// Re-export the component. The content is now accessible at /dashboard/settings
export default SellerProfilePage;